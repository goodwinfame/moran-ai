{
  "name": "moran-ai",
  "private": true,
  "version": "0.1.0",
  "description": "墨染 (MoRan) — AI 长篇小说创作平台",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:services": "docker compose -f docker-compose.dev.yml up -d --wait",
    "dev:app": "pnpm -r --parallel run dev",
    "dev": "pnpm run dev:services && pnpm run dev:app",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --project core-integration --project server-integration",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:dev": "docker compose -f docker-compose.dev.yml up -d",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down",
    "docker:test": "docker compose -f docker-compose.test.yml up -d",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d",
    "docker:prod:down": "docker compose -f docker-compose.prod.yml down",
    "docker:prod:logs": "docker compose -f docker-compose.prod.yml logs -f",
    "docker:build": "docker compose -f docker-compose.prod.yml build",
    "clean": "pnpm -r run clean && rimraf node_modules"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9.28.0",
    "prettier": "^3.5.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.33.0",
    "vitest": "^3.2.0"
  }
}
